docker run --rm -it \
  --net=host \
  --shm-size=1gb \
  --memory="2.8g" \
  --device /dev/fastrpc-cdsp-secure \
  -v $(pwd):/app \
  -e LD_LIBRARY_PATH=/app \
  smolvlm-hexagon:latest \
  python3 vla_test/test_vlm.py
