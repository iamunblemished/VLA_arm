docker run --rm -it \
  --net=host \
  --shm-size=1gb \
  --memory="2.8g" \
  --device /dev/fastrpc-cdsp-secure \
  --device /dev/ion \
  -v $(pwd):/app \
  -e LD_LIBRARY_PATH=/app \
  smolvlm-hexagon:latest \
  python3 test_vlm.py
